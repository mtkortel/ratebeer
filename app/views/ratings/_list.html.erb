<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th>average</th>
  </tr>
  </thead>

  <tbody>
  <% ratings.each do |rating| %>
      <tr>
        <td><%= link_to rating.name, rating %></td>
        <td><%= number_with_precision(rating.average_rating, precision: 1) %></td>
        <td></td>
      </tr>
  <% end %>
  </tbody>
</table>